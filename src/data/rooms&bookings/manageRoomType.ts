// Compatibility export for the room-type form until all consumers import the payload type directly.
// Database actions and React Query hooks live in their own files.
export type { RoomTypePayload } from './createRoomType';
